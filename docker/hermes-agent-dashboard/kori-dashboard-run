#!/command/execlineb -P
foreground { s6-echo "Starting Kori-x Hermes Dashboard..." }
cd /opt/hermes-dashboard
export HOME /root
export HERMES_HOME /opt/data
export HERMES_DASHBOARD_DIR /opt/hermes-dashboard
exec npm run dev
