name: Build Hermes Agent + Dashboard

on:
  push:
    branches: ["1.1"]
    paths:
      - "docker/hermes-agent-dashboard/**"
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: docker/hermes-agent-dashboard
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/hermes-agent-dashboard:latest
            ghcr.io/${{ github.repository_owner }}/hermes-agent-dashboard:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64
